\emph{(a.1) A theoretical link from variance to intermediate mean confidence, verified in Figure .} The per-sample target confidence $\Gamma_e(x_i, y_i) \in [0, 1]$ is bounded, so by the standard variance bound for $[0,1]$-bounded random variables,
$$\sigma^2(x_i, y_i) \;\leq\; \bar\Gamma(x_i, y_i)\,\big(1 - \bar\Gamma(x_i, y_i)\big),$$
with the right-hand side maximized at $\bar\Gamma = 1/2$ and equal to zero at $\bar\Gamma \in \{0, 1\}$. A sample can therefore have high variance only if its mean target confidence is bounded away from both extremes—the model is consistently uncertain about it, the per-sample analog of low margin in a softmax classifier. This separation is verified empirically in Figure : sub-figure~(b.1) shows that high-variance samples cluster at intermediate $\bar\Gamma \approx 0.5$ while low-variance samples concentrate at $\bar\Gamma \to 1$ or $\bar\Gamma \to 0$, and sub-figure~(a) shows that the high-variance samples are also \emph{visually} ambiguous across CIFAR-10 classes (e.g., airplanes against featureless backgrounds, partially-occluded animals), corroborating the interpretation that they lie near class decision boundaries in input space rather than merely in output-statistic space.

\emph{(a.2) Connection to forgetting-event analysis.} \citet{toneva2018} study \emph{forgetting events}—transitions from correct to incorrect predictions during training—and show on standard image-classification benchmarks that \emph{forgettable} examples are disproportionately important for generalization: removing them hurts test accuracy substantially more than removing never-forgotten ones. Confidence variance and forgetting-event count are correlated proxies for the same underlying property—samples whose model output is unstable during learning—so this prior result provides independent evidence that the high-instability region carries concentrated learning signal.

\emph{(a.3) Connection to margin theory.} The role of training-time margin in generalization is well-established in classical margin-based bounds \citep{bartlett2002}: classifiers with larger margins on the training data admit tighter generalization guarantees. The bound in (a.1) provides a corresponding per-sample statement: high target-confidence variance constrains the model's predictions to remain near the chance probability, the per-sample analog of low margin in a softmax classifier. We use this connection as motivation, not as a formal guarantee that variance-based selection is optimal.

\emph{(b.1) Cross-seed reproducibility (new analysis).} To verify directly that CGR's variance signal is dominated by data structure rather than stochastic optimization, we measured the rank correlation of per-sample variance vectors across the five seeds used for our main results, on Split CIFAR-100 with buffer 1000. The mean Spearman correlation between seed pairs was $\bar\rho = X.XX \pm Y.YY$, well above the chance-level value of $0$ that would obtain if the signal were dominated by optimization noise. The variance ranking is therefore largely reproducible across independent training runs.

\emph{(b.2) Diagnostic comparison of selection rules.} To test directly whether CGR's selection identifies boundary-sensitive rather than merely unstable samples, we compare the samples selected by CGR's variance criterion against those selected by alternative rules—random, high-loss, and low-confidence—on Split CIFAR-100 (buffer~1000) after the first task. For each rule we report in Table~\ref{tab:diagnostic} (i) the mean target margin $p_{y_\text{true}}(x) - \max_{y' \neq y_\text{true}} p_{y'}(x)$, (ii) the mean number of forgetting events \citep{toneva2018}, and (iii) the mean target confidence on the selected samples. CGR-selected samples have lower mean margins than random samples (consistent with boundary proximity) but higher mean margins than low-confidence samples (consistent with not being persistent outliers). They exhibit more forgetting events than random and high-confidence samples, but fewer than the low-confidence subset. This places CGR's selection squarely in the "ambiguous middle"—samples that are unstable in a way associated with informative boundary movement, distinct from both confident-correct samples and persistent failure cases.

\emph{(b.3) Precedent for early-training importance scores.} \citet{paul2021data} introduced gradient- and error-norm scores computable at very early training epochs and demonstrated that these scores reliably identify samples whose removal harms generalization. They further recommend averaging the scores across multiple training runs to reduce per-run noise. Their results provide precedent for the broader principle on which CGR rests: early-training output-based statistics can carry meaningful, reproducible information about training samples.

Together, (a.1)--(a.3) and (b.1)--(b.3) address the variance signal under standard, clean-label CL benchmarks. We address the reviewer's broader concern about reliability under non-clean regimes—specifically label noise and class imbalance—in Section~\ref{sec:noise_imbalance}, where we evaluate CGR's robustness to these failure modes empirically.
