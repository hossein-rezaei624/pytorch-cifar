\paragraph{Why early-epoch confidence variance is a useful proxy for boundary-sensitive samples.} CGR's central claim has two components: (a) high target-confidence variance during early training selects samples that are likely to be near class decision boundaries, and (b) this variance signal reflects data structure rather than stochastic optimization noise. We support each component with theoretical and empirical arguments. We do not claim the proxy is a formal guarantee of sample utility; the bound below establishes a necessary condition, and the boundary interpretation is grounded in standard classification theory.

\emph{(a.1) From variance to intermediate confidence, verified in Figure~\ref{fig:Boundary}.} The per-sample target confidence $\Gamma_e(x_i, y_i) \in [0, 1]$ is bounded, so by the standard variance bound for $[0,1]$-bounded random variables,
$$\sigma^2(x_i, y_i) \;\leq\; \bar\Gamma(x_i, y_i)\,\big(1 - \bar\Gamma(x_i, y_i)\big),$$
with the right-hand side maximized at $\bar\Gamma = 1/2$ and equal to zero at $\bar\Gamma \in \{0, 1\}$. A sample can therefore have high variance only if its mean target confidence is bounded away from both extremes—i.e., its predictions fluctuate around an intermediate value, the softmax-confidence analog of low margin in a classifier. This separation is verified empirically in Figure~\ref{fig:Boundary}: sub-figure~(b.1) shows that high-variance samples cluster at intermediate $\bar\Gamma \approx 0.5$ while low-variance samples concentrate at $\bar\Gamma \to 1$ or $\bar\Gamma \to 0$, and sub-figure~(a) shows that the high-variance samples are also visually ambiguous across CIFAR-10 classes.

\emph{(a.2) Connection to margin theory.} The role of training-time margin in generalization is well-established in classical margin-based bounds \citep{bartlett2002rademacher}: classifiers with larger margins on training data admit tighter generalization guarantees. The bound in (a.1) provides a corresponding per-sample statement: high target-confidence variance forces the model's predictions toward intermediate target confidence, the per-sample analog of low margin. We use this connection as motivation, not as a formal guarantee that variance-based selection is optimal.

\emph{(b.1) Cross-seed reproducibility (new analysis).} To test whether CGR's variance signal is dominated by stochastic optimization noise, we measured the rank correlation of per-sample variance vectors across the five seeds used for our main results, on Split CIFAR-100 with buffer 1000. The mean Spearman correlation between seed pairs was $\bar\rho = 0.49 \pm 0.01$ over the ten seed pairs—well above the chance-level value of $0$, and remarkably consistent across pairs. This indicates that the variance ranking is substantially reproducible across independent training runs and is therefore data-dependent rather than seed-specific optimization noise.

\emph{(b.2) Diagnostic comparison of selection rules.} To test directly whether CGR's selection identifies boundary-sensitive rather than merely unstable samples, we compare the samples selected by CGR's variance criterion against four alternatives—random, high-loss, high-confidence, and low-confidence—on Split CIFAR-100 (buffer~1000), averaged over 5 seeds. For each rule, we report in Table XX the mean target margin and mean target confidence over the first $E$ epochs (matching CGR's selection window), and the mean number of forgetting events \citep{toneva2018empirical} over the full task. The five rules separate cleanly into three regimes: a \emph{failure regime} (Low confidence and High loss, with strongly negative mean margin of $\approx\!-0.36$ and high forgetting), an \emph{easy regime} (High confidence, with the highest margin of $+0.37$ and lowest forgetting), and Random, which sits near zero on margin. CGR's selection occupies a position that no other rule produces: positive mean margin ($+0.184$) but lower than the easy regime, intermediate mean confidence ($0.443$, between Random's $0.302$ and High confidence's $0.562$), and forgetting events ($3.07$) lower than Random ($3.99$). This shows that CGR-selected samples are (i) on average correctly classified by the model (positive margin), distinguishing them from persistent failures; (ii) uncertain (intermediate confidence), distinguishing them from easy samples; and (iii) more stable across training than Random, distinguishing them from arbitrary instability. The combination is consistent with informative, learnable boundary samples.

\emph{(b.3) Precedent for early-training importance scores.} \citet{paul2021deep} introduced gradient- and error-norm scores computable at very early training epochs and demonstrated that these scores reliably identify samples whose removal harms generalization. They further recommend averaging the scores across multiple training runs to reduce per-run noise. Their results provide precedent for the broader principle on which CGR rests: early-training importance scores can carry meaningful, reproducible information about training samples.
